<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.or.kids.domain.ca.mail.mapper.EmlSndngMapper">

    <!-- 등록 -->
    <insert id="insertEmlSndng" >
        <selectKey keyProperty="emlSndngSn" resultType="java.math.BigInteger" order="BEFORE">
            SELECT nextval('kids_own.sq_ca_eml_sndng_sn')
        </selectKey>
        INSERT INTO kids_own.tb_ca_m_eml_sndng (
                                          eml_sndng_sn
                                        , eml_ttl
                                        , eml_cn
                                        , sndpty_flnm
                                        , sndpty_eml_addr
                                        , rcvr_flnm
                                        , otsd_eml_dmnd_id
                                        , rcvr_eml_addr
                                        , rcvr_jbps_nm
                                        , sndng_rslt_cd
                                        , atch_file_id
                                        , dsptch_dt
                                        , reg_dt
                                        , rgtr_id
                                        , mdfcn_dt
                                        , mdfr_id
                                ) VALUES (
                                           #{emlSndngSn}
                                         , #{emlTtl}
                                         , #{emlCn}
                                         , #{sndptyFlnm}
                                         , #{sndptyEmlAddr}
                                         , #{rcvrFlnm}
                                         , #{otsdEmlDmndId}
                                         , #{rcvrEmlAddr}
                                         , #{rcvrJbpsNm}
                                         , #{sndngRsltCd}
                                         , #{atchFileId}
                                         , #{dsptchDt}
                                         , NOW()
                                         , #{rgtrId}
                                         , NOW()
                                         , #{mdfrId}
                                         )
    </insert>

    <update id="updateResult">
        UPDATE kids_own.tb_ca_m_eml_sndng
        SET sndng_rslt_cd  = COALESCE(#{sndngRsltCd}, sndng_rslt_cd)
          , mdfcn_dt       = now()
          , mdfr_id        = COALESCE(#{mdfrId}, mdfr_id)
        WHERE eml_sndng_sn = #{emlSndngSn}
    </update>
</mapper>
